using System.Collections;
using System.Collections.Generic;
using Unity.Mathematics;
using UnityEngine;

public class Wireframe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // float time = Time.frameCount;
        // time = time/20 ;
        // float sin = math.sin(time);
        // float cos = math.cos(time);
        //
        // gameObject.transform.localScale = new Vector3(100+5*(sin+cos),100+5*(sin+cos),100+5*(sin+cos));
        //gameObject.transform.localRotation = new quaternion(float3x3.EulerXYZ(time,time,time));

    }
}
